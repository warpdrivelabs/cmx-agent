/target
# cmx-agent-shell/src-tauri 是独立单成员 workspace（不并入根 workspace），自带 target/。
/crates/cmx-agent-shell/src-tauri/target
# 桌面壳运行日志（本地调试产物，不入库）。
/crates/cmx-agent-shell/logs
# tauri-build 自动生成的平台 schema（每次构建按平台重写，勿入库）。
/crates/cmx-agent-shell/src-tauri/gen/
**/*.rs.bk
.DS_Store
**/.DS_Store
# 本地启动配置（若有 API Key，切勿提交）
/.env
/.env.*
*.jsonback
*.bak
node_modules/
# 迁移期 frontend/dist 入库（方案 §15.1：Rust-only 开发流程依赖 dist 存在，build.rs fail-fast）；
# Phase 6 后交 CI 构建时再改为忽略。
!frontend/dist/
