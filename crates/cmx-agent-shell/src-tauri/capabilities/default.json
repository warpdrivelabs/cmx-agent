{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "放行主窗口调用自定义 agent 命令（核心 IPC 默认能力）+ 标题栏拖动。",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-start-dragging"]
}
